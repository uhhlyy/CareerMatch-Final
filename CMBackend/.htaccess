# 1. Enable the rewrite engine
RewriteEngine On

# 2. Capture the Authorization header and pass it to PHP
# This fixes the "401 Unauthorized / No token provided" error
RewriteCond %{HTTP:Authorization} ^(.*)
RewriteRule .* - [e=HTTP_AUTHORIZATION:%1]

# 3. Optional: Prevent people from browsing your folders directly
Options -Indexes